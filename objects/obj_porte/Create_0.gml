// Inherit the parent event
event_inherited();

/// @description Porte ouverte et immobile de base

image_speed = 0;

mask_base = mask_index;

mask_index = spr_porte_petite;
mask_base2 = mask_index;

mask_index = mask_base;